{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "npm run devstart",
      "name": "Run npm start",
      "request": "launch",
      "type": "node-terminal",
      "preLaunchTask": "build",
      "skipFiles": ["<node_internals>/**"],
      "outFiles": ["${workspaceFolder}/server/build/**/*.js"]
    }
  ]
}
